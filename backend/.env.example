PORT=5000
NODE_ENV=development
JWT_SECRET=super_secret_sustainx_key_change_me
JWT_EXPIRES_IN=7d

# Database Settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=sustainx

# Optional full URL connection string
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sustainx

# CORS allowed origins (comma-separated for multiple)
CORS_ORIGIN=http://localhost:3000
